# دليل الإعداد والتشغيل الكامل

## المتطلبات الأساسية

قبل البدء، تأكد من توفر المتطلبات التالية:

- PHP 8.2 أو أحدث
- Composer
- Node.js و npm (للتطوير)
- خادم ويب (Apache/Nginx) أو خادم Laravel المدمج

## خطوات التثبيت التفصيلية

### 1. نسخ المشروع
```bash
git clone https://github.com/Faiz-929/institute-system.git
cd institute-system
```

### 2. تثبيت حزم PHP
```bash
composer install
```

### 3. إعداد ملف البيئة
```bash
cp .env.example .env
```

### 4. توليد مفتاح التطبيق
```bash
php artisan key:generate
```

### 5. إنشاء قاعدة البيانات (إذا كنت تستخدم MySQL)
```sql
CREATE DATABASE institute_db;
```

ثم حدث ملف `.env`:
```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=institute_db
DB_USERNAME=root
DB_PASSWORD=
```

### 6. تشغيل Migrations و Seeders
```bash
# إنشاء الجداول وزرع البيانات الأولية
php artisan migrate --seed

# أو في حالة وجود مشاكل:
php artisan migrate:fresh --seed
```

### 7. إنشاء رابط التخزين
```bash
php artisan storage:link
```

### 8. بناء الأصول (إذا كنت تقوم بالتطوير)
```bash
npm install
npm run build
```

### 9. تشغيل الخادم
```bash
# خادم التطوير
php artisan serve

# أو خادم الإنتاج
php artisan serve --host=0.0.0.0 --port=8000
```

## الوصول للنظام

بعد تشغيل الأوامر، افتح المتصفح على: `http://localhost:8000`

### تسجيل الدخول الأولي

استخدم هذه الحسابات للاختبار:

#### حساب الأدمن (صلاحيات كاملة)
- البريد: `admin@institute.com`
- كلمة المرور: `admin123`

#### حساب معلم
- البريد: `ahmed@institute.com` (أو أي معلم من السيدر)
- كلمة المرور: `teacher123`

#### حساب طالب  
- البريد: `student1@example.com` (إن وجد في السيدر)
- كلمة المرور: `student123`

## إدارة النظام

### الوصول إلى لوحة الإدارة

بعد تسجيل الدخول بحساب الأدمن:
1. انتقل إلى `/admin/users` لإدارة المستخدمين
2. انتقل إلى `/admin/permissions` لإدارة الصلاحيات

### إنشاء مستخدمين جدد

1. انقر على "إضافة مستخدم جديد"
2. أدخل البيانات المطلوبة:
   - الاسم الكامل
   - البريد الإلكتروني (يجب أن يكون فريد)
   - كلمة المرور (8 أحرف على الأقل)
   - الدور (admin/teacher/student)
3. انقر على "إنشاء المستخدم"

### تحديث صلاحيات المستخدمين

1. في صفحة إدارة الصلاحيات
2. اختر المستخدم من القائمة
3. غير الدور المطلوب
4. احفظ التغييرات

## الميزات المتاحة

### للطلاب
- عرض قائمة الطلاب
- إضافة وتعديل بيانات الطلاب
- طباعة قوائم الطلاب (Excel)
- متابعة حضور وغياب الطلاب

### للمعلمين
- إدارة الدرجات والدرجات
- تسجيل الحضور والغياب
- إعداد تقارير الأداء
- إدارة الواجبات والمهام

### للمديرين
- إدارة شاملة لجميع المستخدمين
- تقارير مالية (الرسوم والمدفوعات)
- إدارة الورش والمواد
- إدارة الأصول والعهد

## حل المشاكل الشائعة

### مشكلة: "Class not found"
```bash
composer dump-autoload
php artisan config:clear
php artisan cache:clear
```

### مشكلة: "Database not found"
```bash
# إذا كنت تستخدم SQLite
touch database/database.sqlite

# إذا كنت تستخدم MySQL
# تأكد من إنشاء قاعدة البيانات أولاً
php artisan migrate:fresh --seed
```

### مشكلة: "Permission denied"
```bash
# على Linux/Mac
chmod -R 755 .
chmod -R 775 storage/
chmod -R 775 bootstrap/cache/

# على Windows (قم بتشغيل Command Prompt كمدير)
icacls .\storage /grant Everyone:F /T
icacls .\bootstrap\cache /grant Everyone:F /T
```

### مشكلة: "Encryption key missing"
```bash
php artisan key:generate
```

### مشكلة: "CSRF token mismatch"
تأكد من أن `config/session.php` مُعد بشكل صحيح وأن csrf_token موجود في جميع النماذج.

## النسخ الاحتياطي

### قاعدة البيانات
```bash
# SQLite
cp database/database.sqlite database/backup_$(date +%Y%m%d_%H%M%S).sqlite

# MySQL
mysqldump -u username -p database_name > backup.sql
```

### الملفات
```bash
tar -czf institute_backup_$(date +%Y%m%d).tar.gz . --exclude=node_modules --exclude=vendor --exclude=.git
```

## الإنتاج (Production)

### 1. تحديث إعدادات البيئة
```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com
```

### 2. تحسين الأداء
```bash
php artisan config:cache
php artisan route:cache
php artisan view:cache
php artisan optimize
```

### 3. إعداد خادم الويب
- قم بتكوين Apache/Nginx ليشير إلى مجلد `public/`
- تأكد من حماية ملفات `.env` و `composer.json`

## الدعم والتطوير

### ملفات التوثيق
- `institute-errors-report.md` - تقرير الأخطاء والإصلاحات
- `improvements-summary.md` - ملخص التحسينات المطبقة  
- `institute-system-usage-guide.md` - دليل الاستخدام التفصيلي

### المساهمة
1. Fork المشروع
2. إنشاء branch جديد للميزة
3. تطبيق التغييرات
4. إنشاء Pull Request

## الأمان

⚠️ **تنبيهات مهمة للأمان**:

1. **تغيير كلمات المرور الافتراضية فوراً**
2. **استخدام قاعدة بيانات أكثر أماناً من SQLite في الإنتاج**
3. **تفعيل HTTPS في البيئة الإنتاجية**
4. **عمل نسخ احتياطية دورية**
5. **مراجعة صلاحيات الملفات بانتظام**

## معلومات إضافية

- **التقنيات**: Laravel 12, PHP 8.2, Tailwind CSS 4
- **اللغة**: العربية (RTL Support)
- **الترخيص**: MIT License
- **التوافق**: جميع المتصفحات الحديثة

---

💡 **هل تحتاج مساعدة إضافية؟** راجع ملفات التوثيق أو اطرح استفسارك في Issues.

🎯 **تحديثات مستقبلية**:
- تطبيق موبايل
- تقارير متقدمة  
- إدارة المناهج
- نظام الحجوزات
- تكامل مع أنظمة خارجية